'use client';import Link from 'next/link';import { useAuthStore } from '@/lib/stores/authStore';import { motion } from 'framer-motion';import { useRouter, usePathname } from 'next/navigation';import { LayoutDashboard, Crosshair, ScrollText, Settings, LogOut, FileText, BookOpen, Kanban, Building, Layers, Sun, Moon, Linkedin, Briefcase } from 'lucide-react';import { useTheme } from '@/components/providers/ThemeProvider';const NAV_LINKS = [    { href: '/dashboard', label: 'Command Deck', icon: LayoutDashboard },    { href: '/resume', label: 'Resume', icon: FileText },    { href: '/prep', label: 'Prep', icon: BookOpen },    { href: '/linkedin', label: 'LinkedIn', icon: Linkedin },    { href: '/jobs', label: 'Jobs', icon: Briefcase },    { href: '/settings', label: 'Config', icon: Settings },];export default function Navbar() {    const { user, logout } = useAuthStore();    const router = useRouter();    const pathname = usePathname();    const { theme, toggleTheme } = useTheme();    const handleLogout = () => {        logout();        router.push('/login');    };    if (pathname?.startsWith('/interview/room')) return null;    return (        <motion.nav            initial={{ y: -100 }}            animate={{ y: 0 }}            transition={{ type: "spring", stiffness: 100 }}            className="sticky top-0 z-50 w-full bg-[var(--bg-deep)]/80 backdrop-blur-xl border-b border-[var(--card-border)]"        >            <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">                <div className="flex items-center justify-between h-16">                    {}                    <Link href="/dashboard" className="flex items-center gap-2.5 group">                        <div className="w-8 h-8 rounded-lg bg-gradient-to-br from-purple-500 to-cyan-500 flex items-center justify-center font-bold text-white text-sm shadow-lg shadow-purple-500/20 group-hover:shadow-purple-500/40 transition-all">                            SH                        </div>                        <span className="font-bold text-xl tracking-tight text-[var(--text-primary)] group-hover:text-purple-400 transition-colors">                            Synth<span className="text-gradient-purple">Hire</span>                        </span>                    </Link>                    {}                    <div className="hidden md:flex items-center gap-1">                        {NAV_LINKS.map(link => {                            const isActive = pathname === link.href || pathname?.startsWith(link.href + '/');                            const Icon = link.icon;                            return (                                <Link                                    key={link.href}                                    href={link.href}                                    className={`flex items-center gap-2 px-4 py-2 rounded-lg text-sm font-medium transition-all duration-200 ${isActive                                        ? 'text-white bg-purple-500 border border-purple-500/20 shadow-[0_0_10px_rgba(139,92,246,0.1)]'                                        : 'text-[var(--text-secondary)] hover:text-[var(--text-primary)] hover:bg-[var(--card-border)]'                                        }`}                                >                                    <Icon className="w-4 h-4" />                                    {link.label}                                </Link>                            );                        })}                    </div>                    {}                    <div className="flex items-center gap-4">                        <button                            onClick={toggleTheme}                            className="p-2 rounded-lg bg-[var(--card-bg)] hover:bg-[var(--card-border)] text-[var(--text-secondary)] hover:text-[var(--text-primary)] transition-colors border border-[var(--card-border)] shadow-sm"                            aria-label="Toggle Theme"                        >                            {theme === 'light' ? <Moon className="w-5 h-5 mx-0.5" /> : <Sun className="w-5 h-5 mx-0.5" />}                        </button>                        {user ? (                            <div className="flex items-center gap-4">                                <Link href="/settings" className="flex items-center gap-3 hover:opacity-80 transition-opacity">                                    <div className="text-right hidden sm:block">                                        <p className="text-sm font-medium text-[var(--text-primary)]">{user.full_name}</p>                                        <p className="text-xs text-purple-400">{user.role || (user.account_type === 'company' ? 'Recruiter' : 'Operator')}</p>                                    </div>                                    <div className="w-10 h-10 rounded-full bg-cyber-purple-500/20 border border-cyber-purple-500/30 flex items-center justify-center text-cyber-purple-400 font-bold text-sm uppercase overflow-hidden shrink-0">                                        {user.avatar_url?.startsWith('http') ? (                                            <img src={user.avatar_url} alt="Profile" className="w-full h-full object-cover" />                                        ) : (                                            user.full_name?.charAt(0) || 'U'                                        )}                                    </div>                                </Link>                                <button                                    onClick={handleLogout}                                    className="flex items-center gap-2 bg-[var(--card-bg)] hover:bg-[var(--card-border)] text-[var(--text-secondary)] hover:text-[var(--text-primary)] px-4 py-2 rounded-lg text-sm font-medium transition-all border border-[var(--card-border)]"                                >                                    <LogOut className="w-4 h-4" />                                    Logout                                </button>                            </div>                        ) : (                            <Link href="/login" className="btn-cyber text-sm py-2 px-6">                                Login                            </Link>                        )}                    </div>                </div>            </div>        </motion.nav>    );}