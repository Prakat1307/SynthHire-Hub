'use client';import { usePathname } from 'next/navigation';export default function MainWrapper({ children }: { children: React.ReactNode }) {    const pathname = usePathname();    const isFullScreenApp = pathname?.startsWith('/interview/room') || pathname === '/prep' || pathname?.startsWith('/jobs') || pathname?.startsWith('/linkedin');    if (isFullScreenApp) {        return <>{children}</>;    }    return (        <main className="min-h-screen pt-4 pb-20">            {children}        </main>    );}