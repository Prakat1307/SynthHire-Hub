import { motion, HTMLMotionProps } from 'framer-motion';import { cn } from '@/lib/utils';import React from 'react';import { Loader2 } from 'lucide-react';interface CyberButtonProps extends HTMLMotionProps<"button"> {    variant?: 'primary' | 'secondary' | 'outline' | 'ghost' | 'launch' | 'danger';    isLoading?: boolean;    icon?: React.ReactNode;    children: React.ReactNode;}const CyberButton: React.FC<CyberButtonProps> = ({    children,    className,    variant = 'primary',    isLoading,    icon,    disabled,    ...props}) => {    const variants: Record<string, string> = {        primary: "bg-gradient-to-r from-purple-500 to-cyan-500 text-white shadow-[0_4px_20px_rgba(139,92,246,0.3)] hover:shadow-[0_6px_30px_rgba(139,92,246,0.45)] hover:from-purple-400 hover:to-cyan-400",        secondary: "bg-[var(--card-bg)] border border-[var(--card-border)] text-[var(--text-primary)] hover:border-[var(--card-border-hover)] hover:shadow-[0_4px_20px_var(--glow-purple)]",        outline: "bg-transparent border border-[var(--card-border)] text-[var(--text-secondary)] hover:text-[var(--text-primary)] hover:border-purple-500/40 hover:bg-purple-500/5",        ghost: "bg-transparent hover:bg-black/5 dark:hover:bg-white/5 text-[var(--text-secondary)] hover:text-[var(--text-primary)]",        launch: "bg-gradient-to-r from-purple-500 to-cyan-500 text-white font-bold py-5 px-12 text-lg rounded-2xl shadow-[0_0_30px_rgba(139,92,246,0.4),0_0_60px_rgba(6,182,212,0.2)] hover:shadow-[0_0_50px_rgba(139,92,246,0.6),0_0_100px_rgba(6,182,212,0.3)] animate-pulse-glow",        danger: "bg-red-500/10 border border-red-500/30 text-red-500 hover:bg-red-500/20 hover:border-red-500/50",    };    return (        <motion.button            whileHover={{ scale: 1.03 }}            whileTap={{ scale: 0.97 }}            disabled={isLoading || disabled}            className={cn(                "relative overflow-hidden font-medium py-3 px-6 rounded-full transition-all duration-300 flex items-center justify-center gap-2 cursor-pointer",                variants[variant],                (isLoading || disabled) && "opacity-60 cursor-not-allowed",                className            )}            {...props}        >            {isLoading && <Loader2 className="w-4 h-4 animate-spin" />}            {!isLoading && icon && <span className="w-5 h-5 flex items-center justify-center">{icon}</span>}            {children}        </motion.button>    );};export default CyberButton;